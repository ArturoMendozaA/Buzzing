const fs = require("fs");
//const explorers = JSON.parse(rawdata);
class Reader{
    static obectifying(jsonFile){
        const rawdata = fs.readFileSync(jsonFile);
        JSON.parse(jsonFile)
        returns
    }
}